<UserControl x:Class="DS3Go.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:DS3Go.Views"
             xmlns:vm="clr-namespace:DS3Go.ViewModels"
             xmlns:converters="clr-namespace:DS3Go.Converters">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="20,14" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="DS3" FontSize="22" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                    <TextBlock Text=" Go" FontSize="22" FontWeight="Light" Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
                <TextBlock Text="{Binding StatusText}" FontSize="11"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Ports}" Margin="12,12,12,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:PortViewModel}">
                    <views:PortPanelControl Margin="4" Cursor="Hand">
                        <views:PortPanelControl.InputBindings>
                            <MouseBinding MouseAction="LeftClick"
                                          Command="{Binding DataContext.SelectPortCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                          CommandParameter="{Binding}"/>
                        </views:PortPanelControl.InputBindings>
                    </views:PortPanelControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TabControl Grid.Row="2" Margin="12,12,12,0" Padding="0">
            <TabItem Header="INPUT TESTER">
                <views:InputTesterView DataContext="{Binding InputTester}" Margin="0,8,0,0"/>
            </TabItem>
            <TabItem Header="REMAPEO">
                <views:RemappingView DataContext="{Binding Remapping}" Margin="0,8,0,0"/>
            </TabItem>
        </TabControl>

        <Border Grid.Row="3" Padding="16,6" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="8" Height="8" Margin="0,0,8,0" Fill="{StaticResource PortConnectedBrush}"/>
                <TextBlock Text="{Binding StatusText}" FontSize="11"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Margin="24,0,0,0" FontSize="11"
                           Foreground="{StaticResource TextSecondaryBrush}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="ViGEmBus: No disponible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsViGEmAvailable}" Value="True">
                                    <Setter Property="Text" Value="ViGEmBus: Activo"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
