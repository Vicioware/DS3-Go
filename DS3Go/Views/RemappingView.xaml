<UserControl x:Class="DS3Go.Views.RemappingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:DS3Go.Converters">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="16,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" FontSize="14" FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4">
            <Run Text="Remapeo de Botones"/>
            <Run Text=" · Puerto " FontWeight="Normal" FontSize="12"
                 Foreground="{StaticResource TextSecondaryBrush}"/>
            <Run Text="{Binding PortNumber, Mode=OneWay}" FontWeight="Normal" FontSize="12"
                 Foreground="{StaticResource AccentBrush}"/>
        </TextBlock>

        <TextBlock Grid.Row="1" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="0,0,0,10" TextWrapping="Wrap">
            Haz clic en un botón y luego presiónalo en el mando para asignar la nueva acción.
        </TextBlock>

        <!-- Mapping list -->
        <Border Grid.Row="2" CornerRadius="6" Padding="6"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Entries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="8,5" Margin="0,1" CornerRadius="4">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#16162A"/>
                                            </Trigger>
                                            <DataTrigger Binding="{Binding IsWaiting}" Value="True">
                                                <Setter Property="Background" Value="#1A1830"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Physical button name -->
                                    <TextBlock Grid.Column="0" Text="{Binding PhysicalButtonName}"
                                               VerticalAlignment="Center" FontSize="12"
                                               Foreground="{StaticResource TextPrimaryBrush}"/>

                                    <TextBlock Grid.Column="1" Text="→" Margin="8,0"
                                               VerticalAlignment="Center" FontSize="13"
                                               Foreground="{StaticResource TextSecondaryBrush}"/>

                                    <!-- Target: clickable area to start listening -->
                                    <Button Grid.Column="2" HorizontalAlignment="Left"
                                            Command="{Binding DataContext.StartListeningCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource IconButton}">
                                                <Setter Property="Padding" Value="10,4"/>
                                                <Setter Property="MinWidth" Value="120"/>
                                            </Style>
                                        </Button.Style>
                                        <TextBlock VerticalAlignment="Center" FontSize="12">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="{Binding VirtualButtonName}"/>
                                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                                    <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsWaiting}" Value="True">
                                                            <Setter Property="Text" Value="Presiona un botón..."/>
                                                            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                                            <Setter Property="FontStyle" Value="Italic"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsIdentity}" Value="False">
                                                            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Button>

                                    <!-- Reset single mapping -->
                                    <Button Grid.Column="3" Content="↺" FontSize="14"
                                            ToolTip="Restaurar este botón"
                                            Style="{StaticResource IconButton}" Padding="6,2"
                                            Visibility="{Binding IsIdentity, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}"
                                            Command="{Binding DataContext.ResetSingleMappingCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Bottom bar -->
        <DockPanel Grid.Row="3" Margin="0,10,0,0">
            <Button DockPanel.Dock="Left" Content="Restaurar todos"
                    Style="{StaticResource DangerButton}" Padding="14,6"
                    Command="{Binding ResetMappingsCommand}"/>

            <Button DockPanel.Dock="Right" Content="Cancelar captura"
                    HorizontalAlignment="Right"
                    Style="{StaticResource DarkButton}" Padding="14,6"
                    Visibility="{Binding IsListening, Converter={StaticResource BoolToVis}}"
                    Command="{Binding CancelListeningCommand}"/>
        </DockPanel>
    </Grid>
</UserControl>
